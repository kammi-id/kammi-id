# Docker CI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add GitHub Actions workflow to build and push Docker images to `ghcr.io`, and fix `.dockerignore` to exclude all `.env` variants.

**Architecture:** Two changes — patch `.dockerignore` to broaden env exclusions, add `.github/workflows/docker.yml` as a separate CD workflow (CI stays in `ci.yml`). Image tagged by trigger: `latest`+SHA on main push, semver on tag push, `dev-{sha}` on PR to main.

**Tech Stack:** GitHub Actions, Docker Buildx, `docker/metadata-action@v5`, `docker/build-push-action@v6`, `ghcr.io`

---

### Task 1: Fix `.dockerignore`

**Files:**

- Modify: `.dockerignore`

- [ ] **Step 1: Replace env exclusion line**

Open `.dockerignore`. The current line is:

```
.env*.local
```

Replace it with:

```
.env
.env.*
!.env.example
```

Full file after change:

```
node_modules
.next
.git
.gitignore
.dockerignore
Dockerfile
README.md
LICENSE
tests
playwright
playwright-report
playwright-ct.config.ts
playwright.config.ts
.agents
.claude
.vscode
.gemini
docs
.antigravitycli

# Exclude environment variables
.env
.env.*
!.env.example
```

- [ ] **Step 2: Commit**

```bash
git add .dockerignore
git commit -m "fix: exclude all .env variants from Docker build context"
```

---

### Task 2: Create `docker.yml` workflow

**Files:**

- Create: `.github/workflows/docker.yml`

- [ ] **Step 1: Create the workflow file**

```yaml
name: Docker

on:
  push:
    branches: [main]
    tags: ['v*.*.*']
  pull_request:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha,prefix=dev-,enable=${{ github.event_name == 'pull_request' }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

- [ ] **Step 2: Validate YAML syntax**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/docker.yml'))" && echo "YAML valid"
```

Expected: `YAML valid`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/docker.yml
git commit -m "feat: add Docker build and push workflow to ghcr.io"
```

---

### Task 3: Merge to `dev-20260104`

**Files:** none (git operation only)

- [ ] **Step 1: Fetch and checkout target branch**

```bash
git fetch origin dev-20260104
```

- [ ] **Step 2: Note current branch**

```bash
git branch --show-current
```

Expected: `claude/great-franklin-fb3c14` (the worktree branch)

- [ ] **Step 3: Merge into `dev-20260104`**

```bash
git checkout dev-20260104
git merge claude/great-franklin-fb3c14 --no-ff -m "feat: docker CI — build and push to ghcr.io"
```

- [ ] **Step 4: Verify merge result**

```bash
git log --oneline -5
git diff origin/dev-20260104..HEAD --stat
```

Expected: commits from this branch appear on top, changed files include `.dockerignore`, `.github/workflows/docker.yml`, `docs/`.

- [ ] **Step 5: Push to remote**

```bash
git push origin dev-20260104
```

- [ ] **Step 6: Switch back to worktree branch**

```bash
git checkout claude/great-franklin-fb3c14
```
