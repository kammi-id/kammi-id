// Playwright CT entry point — required for @playwright/experimental-ct-react.
// Components are mounted here during component tests.
// Note: globals.css uses Tailwind v4 (@import 'tailwindcss') which requires
// @tailwindcss/vite plugin. CT tests check behavior/content, not styling.
